import React from "react";

function Thumbs({ thumbs }) {
  console.log({ thumbs });
  // if(typeof())
  // let thumb = thumbs.forEach(element => {
  //   <img src={thumbs ? thumbs.urls.thumb : ""} />;
  // });
  return "";
  // return thumb.join("");
}

export default Thumbs;
